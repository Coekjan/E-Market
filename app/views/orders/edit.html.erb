<h1 class="text-center">编辑订单</h1>

<div class="card orderCard">
  <div class="card-header">订单信息</div>
  <div class="card-body">
    <%= render 'edit_form', order: @order, url: customer_order_path(@order.customer, @order) %>
  </div>
</div>
